module.exports = {
  plugins: {
    // Tailwind CSS is typically run as a PostCSS plugin
    'tailwindcss': {},
    // Autoprefixer adds vendor prefixes for better browser compatibility
    'autoprefixer': {},
  },
}
